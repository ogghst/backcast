# DO Phase: E06-U08 Branch Archival UI (Finalization)

## Progress Summary

- **Tests written:** 2
- **Tests passing:** 2
- **Files modified:** 5 (change_orders.py, test_change_order_archive.py, useArchiveChangeOrder.ts, WorkflowActions.tsx, plus generated client files)
- **Coverage delta:** ~0%

## TDD Cycle Log

| Test Name                                | RED Reason                  | GREEN Implementation                                       | REFACTOR Notes               | Date       |
| ---------------------------------------- | --------------------------- | ---------------------------------------------------------- | ---------------------------- | ---------- |
| `test_archive_implemented_change_order`  | HTTP 404 (endpoint missing) | Added `/archive` route calling service method              | Fixed typo in assertion test | 2026-02-25 |
| `test_archive_active_change_order_fails` | HTTP 404 (endpoint missing) | Service throws ValueError inside route, mapped to HTTP 400 | -                            | 2026-02-25 |

## Files Changed

- `backend/app/api/routes/change_orders.py`: Added the `POST /{id}/archive` endpoint logic.
- `backend/tests/api/routes/change_orders/test_change_order_archive.py`: API tests for the new endpoint ensuring proper error codes and deletion state.
- `frontend/src/api/generated/*`: openapi spec built and client refreshed.
- `frontend/src/features/change-orders/api/useArchiveChangeOrder.ts`: New React Query hook for the action.
- `frontend/src/features/change-orders/components/WorkflowActions.tsx`: Button, tooltip, and confirmation modal added exclusively to `implemented` and `rejected` states.

## Decisions Made

- Use `react-router-dom` `useNavigate` to redirect to `/change-orders` immediately after clicking `Archive` via the confirmation modal, preventing query re-fetching errors on deleted states.
- Follow logic structure mimicking `useMergeChangeOrder` to keep hooks decoupled from React components.

## Next Steps

1. Transition to the CHECK phase to inspect quality and review technical debt items.
