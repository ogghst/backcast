# DO Phase: Standardize Bitemporal List Operations

## Work Log

### Date: 2026-01-10

**Status**: 🚀 Starting

## Implementation Log

### 2026-01-10

- **Analysis**: Identified that "Time Travel" for lists implies "Valid Time Travel" (browsing history with current knowledge), whereas generic `get_as_of` was enforcing strict "System Time Travel".
- **Refactoring**: Added `_apply_bitemporal_filter` to `TemporalService` to standardize logic.
- **TDD**: Created `test_project_service_temporal.py` to verify "Zombie" records (deleted items) reappear when querying past valid time.
- **Implementation**:
  - Updated `ProjectService.get_projects` to use standardized filter.
  - Updated `CostElementService.get_cost_elements` to use standardized filter.
  - Updated `WBEService.get_wbes` to use standardized filter.
- **Verification**: Confirmed Frontend hooks (`useProjects`, `useCostElements`, `useWBEs`) already transmit `asOf` parameter. Confirmed API routers accept it.

**Goals**:

1.  Verify/Extract strict bitemporal filtering logic from `WbEsService`.
2.  Implement standardization in `ProjectsService`.
3.  Implement standardization in `CostElementsService`.

---

**Running Validation**: Appending as we progress.
