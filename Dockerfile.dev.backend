# Development Dockerfile for FastAPI Backend
# Includes hot-reload support and dev dependencies

FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim

# Set working directory
WORKDIR /app

# Install system dependencies for development
RUN apt-get update && apt-get install -y \
    curl \
    postgresql-client \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# Copy dependency files (uv.lock enforces pinned versions)
COPY backend/pyproject.toml backend/uv.lock backend/README.md* ./

# Install ALL dependencies (including dev) -- --locked fails the build if uv.lock is out of sync
RUN uv sync --locked --all-extras

# Expose port
EXPOSE 8020

# Default command (override in docker-compose)
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8020", "--reload"]
