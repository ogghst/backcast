{
  "mcpServers": {
    "db": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://backcast:backcast@localhost:5433/backcast_evs"
      ]
    }
  }
}
